<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Not used by widget directly, but needed for HomeLocationActivity -->
    <style name="Theme.WeatherWidget" parent="@android:style/Theme.DeviceDefault.Dialog" />
    <style name="Theme.Transparent" parent="@android:style/Theme.DeviceDefault.Dialog" />
</resources>
